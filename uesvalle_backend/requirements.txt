# Django Framework y extensiones
Django>=4.2.0,<5.0.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
django-filter>=23.0

# Base de datos
# PostgreSQL (Supabase)
psycopg[binary]>=3.1.0

# MySQL
mysqlclient>=2.2.0

# Variables de entorno
python-dotenv>=1.0.0

# Task Queue y Message Broker
celery[redis]==5.3.4  # Celery con soporte Redis
redis>=4.5.2,<5.0.0   # Cliente Redis (compatible con Celery 5.3.4)
flower==2.0.1         # Dashboard de monitoreo para Celery (opcional pero recomendado)

# ETL y análisis de datos
pandas>=2.0.0
openpyxl>=3.1.0      # Para leer archivos Excel
xlsxwriter>=3.1.0    # Para escribir archivos Excel con streaming

# Reportes y generación de documentos
reportlab>=4.0.0     # Para generar PDFs de alto rendimiento
Pillow>=10.0.0       # Para soportar imágenes en reportlab

# SQLAlchemy para operaciones avanzadas de BD
SQLAlchemy>=2.0.0

# Supabase cliente (opcional, para REST API)
supabase>=1.0.0

# Utilidades
requests>=2.31.0
python-dateutil>=2.8.0

# Logging y monitoreo
structlog>=23.0.0

# Validación y serialización
marshmallow>=3.19.0

# Testing
pytest>=7.4.0
pytest-django>=4.5.0
pytest-cov>=4.1.0
factory-boy>=3.3.0

# Desarrollo
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0

# Producción
gunicorn>=21.0.0
whitenoise>=6.5.0

# Seguridad
cryptography>=41.0.0